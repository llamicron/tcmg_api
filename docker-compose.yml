version: '3'
services:
  redis:
    image: redis:4.0.5-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - 6379:6379
    restart: 'always'

  api:
    build: ./api
    links:
      - redis
    ports:
      - "5000:5000"
    restart: always
